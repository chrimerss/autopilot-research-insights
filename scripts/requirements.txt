# Pinned to audited-clean versions (matches the SHA-pinned actions' rigor).
# Bump deliberately; CI re-resolves on every run.
# The [bedrock] extra pulls boto3/botocore so the AWS Bedrock provider works;
# it's harmless when using the direct Anthropic API.
anthropic[bedrock]==0.105.2
pymupdf==1.27.2.3
python-slugify==8.0.4
PyYAML==6.0.3
boto3==1.43.18
botocore==1.43.18
