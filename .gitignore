# Jekyll build output
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Ruby / Bundler
.bundle/
vendor/
vendor/bundle/

# Python
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# OMC scaffolding — NEVER commit (locked decision D1: keeps the interview
# transcript, specs, plans, and internal state off the public repo)
.omc/

# Local Claude Code / editor state
.claude/

# Published-paper PDFs: used locally to build _data/publications.yml, but NOT
# committed — they're ~500MB of publisher final PDFs (repo bloat + copyright).
# Add main-text as published/<slug>.md instead for richer insight grounding.
published/*.pdf

# macOS
.DS_Store
